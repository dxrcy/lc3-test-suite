; comment at start
.ORIG x3000 ; comment after .ORIG
    halt ; comment after instruction
    halt;comment not separated by a space
;   halt instruction and .ORIG directive in a comment
; full comment line
.END ; comment at end
